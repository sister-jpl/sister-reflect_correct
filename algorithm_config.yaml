# DO NOT DELETE
# THIS CONFIG IS AUTO-GENERATED BY ADE UI
algorithm_name: sister-reflect_correct
algorithm_version: sister-dev
repository_url: https://github.com/EnSpec/sister-reflect_correct.git
docker_container_url: mas.dit.maap-project.org/root/maap-workspaces/base_images/vanilla:dit

# fill out these fields
# explain what this algorithm does
algorithm_description: Apply BRDF, topographic and glint correction algorithms to reflectance data.
# path to the build script for installing and building the algorithm in the docker image
build_command: sister-reflect_correct/install.sh
# path to the wrapper script for running the algorithm
run_command: sister-reflect_correct/pge_run.sh
# set a storage value in GB or MB or KB, e.g. "100GB", "20MB", "10KB"
disk_space: 50GB
inputs:

  file:
    - name: l1b_obs
      description:
      required: True
    - name: l2a_rfl
      description:
      required: True
  config:
    - name: CRID
      data_type:
      default: '000'
      description:
